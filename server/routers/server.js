const { Router } = require('express')
const { conn, m_server } = require('../data')

let router = new Router()

router.get('/', (req, rsp, next) => {

})

router.put('/', (req, rsp, next) => {

})

router.post('/', (req, rsp, next) => {

})

router.get('/all', (req, rsp, next) => {

})

const server = router

module.exports = server
